# 31 平台真实采集改造说明

> 生成时间：2026-09-23　・　项目：项目 008 采集（price_collector / 比价探针 PriceProbe）
> 改造目标：把 31 个电商平台全部接成
>
> **可插拔真实数据源**
>
> ，
>
> **彻底移除示例 /mock 数据**
>
> ，默认且唯一采集路径为真实抓取（live）。



***

## 一、改造总览



| 维度    | 改造前                                | 改造后                                                       |
| ----- | ---------------------------------- | --------------------------------------------------------- |
| 平台数   | 3（jd/taobao/pdd）                   | **31**（全覆盖）                                               |
| 数据源   | mock 示例（默认）+ auto 自动回退 mock + live | **仅 live 真实抓取**，零伪造                                       |
| 注册表   | 写死 3 平台                            | **动态发现** sources/ 下所有源，按权威键清单注册                           |
| 空结果行为 | 不足 3 条自动补 mock                     | 返回 `total=0 / products=[] / source_mode="live"`，**绝不补数据** |
| 前端    | 3 个固定 chip + mock 下拉               | **31 平台扁平多选**，无 mock 选项，空态有提示                             |
| 部署    | EdgeOne Pages + 云函数                | 链路不变，云函数默认 live，build\_deploy 自动打包全部新源                    |

**重要诚实声明**：受纯标准库（urllib）+ 标准 UA + 不破解验证码 / 不绕过反爬的合规约束，本机无 Cookie 实测时 31 个平台**当前均返回 0 条**—— 这不是 bug，而是各平台真实反爬现状（详见下表）。每个源的请求 URL、字段解析正则都已按各平台真实页面结构写好，**一旦在运行环境注入对应登录 Cookie，或后续接入浏览器渲染（Playwright），即可直接出数据，无需改架构**。



***

## 二、31 平台逐平台明细

键名即 `price_collector/sources/<键>.py`，Cookie 环境变量为 `PC_COOKIE_<大写键>`（为空时回退通用 `PC_COOKIE`）。

### 31 平台明细



| 键            | 中文名         | 公司           | 入口 URL                                                                                  | 可抓性评级      | 所需 Cookie                | 本机实测 | 空结果原因                                   |
| ------------ | ----------- | ------------ | --------------------------------------------------------------------------------------- | ---------- | ------------------------ | ---- | --------------------------------------- |
| jd           | 京东          | 京东集团         | [search.jd.com/Search](https://search.jd.com/Search)                                    | 强反爬可能空结果   | PC\_COOKIE\_JD           | 0    | 反爬壳，价格 / 列表走异步接口                        |
| taobao       | 淘宝          | 阿里巴巴         | [s.taobao.com/search](https://s.taobao.com/search)                                      | 需登录 Cookie | PC\_COOKIE\_TAOBAO       | 0    | React/ICE 框架壳，数据需登录态异步接口                |
| pdd          | 拼多多         | PDD Holdings | [mobile.yangkeduo.com/search\_result](https://mobile.yangkeduo.com/search_result)       | 仅 App      | PC\_COOKIE\_PDD          | 0    | 移动端壳 + 设备指纹，App 闭环                      |
| tmall        | 天猫          | 阿里巴巴         | [list.tmall.com/search\_product.htm](https://list.tmall.com/search_product.htm)         | 需登录 Cookie | PC\_COOKIE\_TMALL        | 0    | ICE 壳，需登录态                              |
| vip          | 唯品会         | 唯品会          | [category.vip.com/suggest.php](https://category.vip.com/suggest.php)                    | 需浏览器自动化    | PC\_COOKIE\_VIP          | 0    | 纯 SPA 壳，无内嵌商品 JSON                      |
| suning       | 苏宁易购        | 苏宁易购         | [search.suning.com/](https://search.suning.com/)                                        | 需登录 Cookie | PC\_COOKIE\_SUNING       | 0    | 页面大但价格走带签名 AJAX                         |
| douyin       | 抖音电商        | 字节跳动         | [douyin.com/search](https://douyin.com/search)                                          | 需浏览器自动化    | PC\_COOKIE\_DOUYIN       | 0    | 强混淆 JS VM，无商品字段                         |
| kuaishou     | 快手电商        | 快手           | [kuaishou.com/search](https://kuaishou.com/search)                                      | 需浏览器自动化    | PC\_COOKIE\_KUAISHOU     | 0    | Next/Vite SPA 壳                         |
| wechat       | 微信小店        | 腾讯           | 视频号助手后台                                                                                 | 仅 App      | PC\_COOKIE\_WECHAT       | 0    | 微信闭环，无公开 PC 搜索端点                        |
| xiaohongshu  | 小红书         | 小红书          | [xiaohongshu.com/search\_result](https://xiaohongshu.com/search_result)                 | 需登录 Cookie | PC\_COOKIE\_XIAOHONGSHU  | 0    | 登录墙，结果走加密接口                             |
| c1688        | 1688        | 阿里巴巴         | [s.1688.com/selloffer/offer\_search.htm](https://s.1688.com/selloffer/offer_search.htm) | 需登录 Cookie | PC\_COOKIE\_1688         | 0    | 未登录 302 跳登录页                            |
| meituan      | 美团闪购        | 美团           | [waimai.meituan.com](https://waimai.meituan.com)                                        | 仅 App      | PC\_COOKIE\_MEITUAN      | 0    | 无 Cookie 直连 403，App 闭环需定位 + 登录          |
| jdnow        | 京东秒送        | 京东集团         | [jddj.com/search](https://jddj.com/search)                                              | 需浏览器自动化    | PC\_COOKIE\_JDNOW        | 0    | JS 壳，商品经异步 API + 定位 + 风控                |
| tbflash      | 淘宝闪购        | 阿里巴巴         | 淘宝闪购 / 饿了么                                                                              | 仅 App      | PC\_COOKIE\_TBFLASH      | 0    | 已并入淘宝 App / 饿了么，无独立公开搜索                 |
| hema         | 盒马          | 阿里巴巴         | [freshhema.com/search](https://freshhema.com/search)                                    | 需浏览器自动化    | PC\_COOKIE\_HEMA         | 0    | JS 壳，mtop API + 门店定位 + 签名               |
| dewu         | 得物          | 上海识装科技       | [dewu.com/search](https://dewu.com/search)                                              | 需浏览器自动化    | PC\_COOKIE\_DEWU         | 0    | Next.js，**NEXT\_DATA** 商品数组为空，需风控 token |
| yanxuan      | 网易严选        | 网易           | [you.163.com/search](https://you.163.com/search)                                        | 需浏览器自动化    | PC\_COOKIE\_YANXUAN      | 0    | 壳，商品异步拉取                                |
| miyoupin     | 小米有品        | 小米集团         | [xiaomiyoupin.com/search](https://xiaomiyoupin.com/search)                              | 需浏览器自动化    | PC\_COOKIE\_MIYOUPIN     | 0    | 壳，商品异步拉取                                |
| xianyu       | 闲鱼          | 阿里巴巴         | [goofish.com/search](https://goofish.com/search)                                        | 强反爬可能空结果   | PC\_COOKIE\_XIANYU       | 0    | 阿里 baxia 风控，接口需 uab/umid/et 签名          |
| zhuanzhuan   | 转转          | 转转集团         | [zhuanzhuan.com](https://zhuanzhuan.com)                                                | 仅 App      | PC\_COOKIE\_ZHUANZHUAN   | 0    | PC 官网仅落地页，/search 等 404                 |
| tmallglobal  | 天猫国际        | 阿里巴巴         | [pages.tmall.com](https://pages.tmall.com)（WOW SPA）                                     | 需登录 Cookie | PC\_COOKIE\_TMALLGLOBAL  | 0    | 旧搜索 URL 已 404，迁至 SPA，静态无商品              |
| jdglobal     | 京东国际        | 京东集团         | [jd.hk](https://jd.hk)（复用 [search.jd.com](https://search.jd.com)）                       | 强反爬可能空结果   | PC\_COOKIE\_JDGLOBAL     | 0    | 未登录返回滑块验证页                              |
| kaola        | 考拉海购        | 阿里巴巴         | [kaola.com](https://kaola.com)                                                          | 强反爬可能空结果   | PC\_COOKIE\_KAOLA        | 0    | 域名疑已迁移，当前网络不可达                          |
| douyinglobal | 抖音全球购       | 字节跳动         | [douyin.com/search](https://douyin.com/search)                                          | 仅 App      | PC\_COOKIE\_DOUYINGLOBAL | 0    | SPA 壳，PC 不开放全球购商品搜索                     |
| temu         | Temu        | PDD Holdings | [temu.com/search\_result.html](https://temu.com/search_result.html)                     | 需浏览器自动化    | PC\_COOKIE\_TEMU         | 0    | 商品走带签名 FaaS 接口，静态无 goods                |
| shein        | SHEIN       | SHEIN        | [shein.com/new/search.html](https://shein.com/new/search.html)                          | 强反爬可能空结果   | PC\_COOKIE\_SHEIN        | 0    | WAF/Cloudflare 直返 403                   |
| tiktokshop   | TikTok Shop | 字节跳动         | [shop.tiktok.com/view/search](https://shop.tiktok.com/view/search)                      | 需登录 Cookie | PC\_COOKIE\_TIKTOKSHOP   | 0    | 直连超时，需登录 / 风控放行                         |
| aliexpress   | AliExpress  | 阿里巴巴         | [aliexpress.com/wholesale](https://aliexpress.com/wholesale)                            | 强反爬可能空结果   | PC\_COOKIE\_ALIEXPRESS   | 0    | x5sec 风控跳转登录 / 滑块                       |
| shopee       | Shopee      | Sea Group    | [shopee.com/search](https://shopee.com/search)                                          | 需浏览器自动化    | PC\_COOKIE\_SHOPEE       | 0    | SPA 壳，商品走受签名保护的 API                     |
| lazada       | Lazada      | 阿里巴巴         | [lazada.com](https://lazada.com).ph/catalog                                             | 强反爬可能空结果   | PC\_COOKIE\_LAZADA       | 0    | x5sec captcha 跳转页                       |
| amazon       | Amazon      | 亚马逊          | [amazon.com/s?k=](https://amazon.com/s?k=)                                              | 强反爬可能空结果   | PC\_COOKIE\_AMAZON       | 0    | 直连 503 反爬；已按 data-asin 区块写好解析           |



***

## 三、删除 / 改动清单

### 已删除（示例数据路径）



* `price_collector/sources/mock.py`（MockSource 示例生成器）

* `data/sample.json`、`data/sample.csv`（示例数据）

* `web/static_demo.html`（内嵌 36 条示例的静态演示页）

* `build_static_demo.py`（静态演示页生成器）

* cli 的 `demo` 子命令

* SourceFactory 的 MockSource 未知平台回退（未知平台直接跳过 / 抛错，不再兜底）

* pipeline 的 auto→mock 兜底逻辑

* server /cli/ 云函数的 `mock` 默认源与 mock/auto 分支

### 已改动



* `price_collector/sources/base.py`：动态注册表 `init_registry`（按 registry.ALL\_PLATFORMS 发现），去除 mock 回退

* `price_collector/sources/registry.py`（新增）：31 平台键 / 中文名 / 公司单一事实源

* `price_collector/sources/__init__.py`：从 registry 派生导出，不再 import mock

* `price_collector/pipeline.py`：唯一真实路径 live；平台清单用注册表；不传全局 cookie，各源自读 `PC_COOKIE_<KEY>`

* `price_collector/analysis.py`：PLATFORM\_ORDER/PLATFORM\_NAMES 从注册表派生，覆盖全部平台

* `server.py`：默认 source=live，平台白名单从注册表派生，移除 static\_demo 白名单

* `cli.py`：`--source` 默认且仅 live，平台 choices 动态，移除 demo 子命令

* `cloud-functions/api/search.py`：默认 live，平台白名单从注册表派生

* `web/index.html` + `app.js` + `style.css`：31 平台扁平多选 chips、品牌色、移除 mock 下拉、首屏引导与空态提示

* jd/taobao/pdd：cookie 读取改为专属 `PC_COOKIE_<KEY>` 回退 `PC_COOKIE`，补全可抓性 docstring

* 新增 28 个源文件（见上表）

### 未改动



* `Dockerfile` / `deploy.ps1`（用户不使用 Docker）

* `.github/workflows/deploy.yml`（push main 自动部署链路不变）

* `live.json`、`verify_live_full.py`（用户既有本地脚本，保留）



***

## 四、剩余限制与建议（重要）



1. **云函数有外网，但平台反爬仍需 Cookie**：2026-09-23 实测（`GET /api/netcheck`，见下）确认 EdgeOne 云函数**可正常访问公网**（example.com / baidu.com 均 200），此前"云函数无外网"的判断已证伪。线上 `/api/search` 返回空的原因是**各平台反爬拦截（无 Cookie）**，不是网络不可达。要让线上稳定出真实数据，二选一：

* **方案 B（现在可行，推荐）**：直接在 EdgeOne 控制台或 CLI（`edgeone makers env set PC_COOKIE_JD "<cookie>" -e production`）配置各 `PC_COOKIE_*` 环境变量即可，无需改代码。已实测云端无 Cookie 时 Amazon 偶发可出数据（约 4 条，不稳定），其余平台全部被拦；注入 Cookie 后按各源"所需 Cookie"列逐个放行。

* **方案 A（备选，免费）**：在一台免费可出网的云服务器（如 GitHub Codespaces / 免费 VPS）上跑 `python server.py`，把前端的 `/api/search` 指向该地址，并在该机器上 `setx PC_COOKIE_JD "..."` 等注入各平台 Cookie。

1. **纯标准库抓不动强 JS 渲染站**：表中标注 "需浏览器自动化" 的平台（得物 / 严选 / 有品 / 抖音 / 快手 / 盒马 / Temu/Shopee 等）要出数据，建议后续引入 Playwright 渲染层（在新源类里调用，保持 BaseSource 接口不变），或改用各平台开放 API。

2. **Cookie 注入方式**：本机调试 `$env:PC_COOKIE_JD="pin=...; ..."` 后 `python cli.py search 蓝牙耳机`；生产用环境变量。

3. **合规边界**：仅用公开网页端点 + 标准 UA，未做验证码破解 / 代理池 / 模拟点击；被拦截即如实返回空。



***

## 五、本次验证记录



* `python -m compileall price_collector server.py cli.py`：通过

* 注册表动态发现：**31/31 平台**全部注册成功

* `python cli.py search 蓝牙耳机 --platform jd`：`source_mode=live`，0 条，不伪造

* 本地 `server.py`：`/api/health` 200；`/api/search` 返回 `total=0 / source_mode=live`

* `node --check web/app.js`：通过

* `python build_deploy.py`：deploy/ 结构校验通过，deploy 副本注册表 31 平台、mock.py 已不存在



***

## 六、方案 A/B 实施记录（2026-09-23 追加）

### 方案 A：免费联网后端 + 前端可配置指向

**代码侧（已入库并本地验证）**



* `server.py`：补 `OPTIONS` 预检（204 + CORS 头），所有响应路径统一带 `Access-Control-Allow-Origin:*`；新增 `--host/--port` 参数（仍兼容 `HOST/PORT` 环境变量）；新增 `GET /api/platforms` 供前端动态拉取 31 平台清单。

* `web/app.js`：API 地址可配置，优先级 `window.PP_API_BASE` → URL `?api=https://...`（写入 localStorage）→ `localStorage['pp-api-base']` → 同域（默认不变）。跨域 GET 为简单请求，配合后端 CORS 即可。

* 部署配置：`render.yaml`（Render 免费 Web Service 蓝图）、`.devcontainer/devcontainer.json`（GitHub Codespaces，自动装 playwright+chromium、转发 8765）。

* 本地完整链路 curl 验证：`/api/health` 200；`/api/platforms` 返回 5 组；`OPTIONS /api/search` 204 且带 CORS 头；`/api/search` 返回 `total=0 / source_mode=live`。

**推荐排序（面向中国 / 宁波、**[github.com](https://github.com)**&#x20;可达、gh 已认证）**



1. **GitHub Codespaces**（最省、无需新账号）：`gh auth refresh -h github.com -s codespace` 授权后，`gh codespace create` → 终端 `python server.py --host 0.0.0.0` → `gh codespace ports visibility 8765:public -c <名>` 得到 `https://<名>-8765.app.github.dev`。

2. **Render 免费 Web Service**：仓库根已有 `render.yaml`，控制台 New→Blueprint 选本仓库即可；免费版闲置休眠、冷启动 30\~50s。

3. 备选：Koyeb/Zeabur 免费额度、腾讯 / 阿里云免费试用轻量服务器（需实名，步骤更多）。

**待用户操作（无法自动化）**



* 本机 gh token 当前无 `codespace` scope，需在浏览器完成 `gh auth refresh -h github.com -s codespace`；

* Render 需注册账号（可能要求绑卡 / 验证）。

* 拿到后端 URL 后，前端访问 `https://<你的EdgeOne站>/?api=https://<后端URL>` 即指向外部后端（会自动记住）。

### 方案 A 上线记录（2026-09-23 实测）

**Codespaces 后端已上线并实测通过：**`https://obscure-pancake-49rp9j9ppvq27jvj-8765.app.github.dev`



* 部署方式：`gh codespace create -R afdk1991/proj-08 -b main --machine basicLinux32gb` → 容器内 `cd /workspaces/proj-08 && nohup python server.py --host 0.0.0.0 --port 8765 >/tmp/pp-server.log 2>&1 &` → `gh codespace ports visibility 8765:public -c <名>`。

* 实测（经公网 URL）：`/api/health` 200 `{"status":"ok"}`；`/api/platforms` 返回 5 组 31 平台；`OPTIONS /api/search` 204 且带 `Access-Control-Allow-Origin:*`；`/api/search?keyword=iphone&platform=amazon` 返回 `source_mode=live / total=0`（未注入 Cookie，如实空）。

* 参数名注意：后端与前端统一使用 `keyword=`（`q=` 会被忽略并回退默认词）。

* 前端接入地址：`https://price-collector-demo-j4ml6bw0.edgeone.cool?eo_token=<部署签发>&eo_time=<时间>&api=https://obscure-pancake-49rp9j9ppvq27jvj-8765.app.github.dev`。

* **唯一生产站（2026-09-23 统一）**：`https://price-collector-demo-j4ml6bw0.edgeone.cool`（带 `eo_token` 访问）为唯一正式入口；`.github/workflows/deploy.yml` 自动部署目标为 `price-collector-demo`。原 `priceprobe-full` 项目已从 EdgeOne 控制台删除，不再使用。云函数出网已验证（`/api/netcheck` 访问 example.com / baidu.com 均 200）。

* 注意事项：


  * Codespaces 闲置约 30 分钟自动休眠（免费配额有限）；`.devcontainer/devcontainer.json` 的 `postStartCommand` 会在每次启动时自动拉起 server，醒来即恢复；

  * 注入 Cookie：`gh codespace ssh -c <名> -- "export PC_COOKIE_JD='...' && nohup python server.py --host 0.0.0.0 --port 8765 >/tmp/pp-server.log 2>&1 &"`，或写入 `.devcontainer/devcontainer.json` 的 `containerEnv` 后重建；

  * Render 蓝图（`render.yaml`）仍为备选，如需第二条后端可按文档操作。

  * **重要运维要点：Codespaces 每次休眠唤醒后，端口公开设置会重置为私有**，需重新执行 `gh codespace ports visibility 8765:public -c <名>`；server 由 `postStartCommand` 自动拉起，无需手动重启。

### 方案 B：Playwright 可选渲染层



* 新增 `price_collector/sources/_browser.py`：封装 Playwright sync API（headless Chromium、标准 UA、cookie 注入），开关 `PRICE_COLLECTOR_BROWSER=1`；**未装 playwright / 未开开关 / 任何异常一律静默返回空串**，绝不崩溃、绝不伪造。

* 已接入 `amazon.py`：直连解析为空且开关打开时，用渲染后 HTML 复用现有 `data-asin` 区块解析。其余 "需浏览器自动化" 平台（vip/douyin/kuaishou/dewu/yanxuan/miyoupin/hema/jdnow/temu/shopee）按同一 `_browser.render_html(...)` 模式即可接入。

* `requirements.txt` 标注 playwright 为**可选**依赖；`.github/workflows/browser-smoke.yml` 为手动触发 job（装 playwright+chromium，对 amazon/shopee/temu/vip 之一跑 "渲染→解析出数"），不阻塞 EdgeOne 部署。

* 本机 Python 3.14.7 无 playwright wheel，按既定约定**渲染出数验证在 CI（3.12）/ Codespaces 内进行**；本地已验证：开关开、未装 playwright 时 `render_html` 返回空串、`amazon.fetch` 返回 0 且不抛错（优雅降级成立）。

* 合规边界不变：仅渲染公开页、标准 UA、不破解验证码 / 不模拟点击 / 不代理池，被拦截如实返回空。

### 方案 B 实施记录（2026-09-23 追加 · 云函数外网实测）

* 新增诊断端点 `cloud-functions/api/netcheck.py`（`GET /api/netcheck`）：验证云函数能否访问公网。**实测结果：example.com / baidu.com 均 200 可达** —— EdgeOne 云函数具备外网能力，"云函数无外网"旧结论证伪。
* 云端无 Cookie 抓取实测（经线上 `/api/search`，keyword=蓝牙耳机/wireless earbuds）：jd/taobao/pdd/temu/douyin 均 `total=0`（反爬拦截）；**Amazon 偶发返回约 4 条**（不稳定，波动于 0~4 条）。
* 结论：线上要稳定出真实数据，只需按上表为各平台配置 `PC_COOKIE_*` 环境变量（CLI：`edgeone makers env set PC_COOKIE_JD "<cookie>" -e production`；控制台：项目 → 函数环境变量）。未配置前，部分反爬较弱的平台可能偶发出数，属正常波动。
* 前端商品图片列已于 2026-09-23 上线：对比表新增「图片」列（52px 缩略图、懒加载、无图/失败降级占位），数据模型 `Product.image` 各源早已解析，出数据即显示图片。

### 方案 B 实施记录（2026-09-25 追加 · 网易严选浏览器渲染跑通）

* **首个 JS 重站经真实浏览器渲染出数 —— 网易严选**。用 Playwright MCP 驱动真实 Chrome 渲染 `https://you.163.com/search?keyword=蓝牙耳机`，**未登录**即可见商品；结构化提取共 **56 条真实商品**，每条含名称 / 现价 / 原价 / 主图（`yanxuan-item.nosdn.127.net`）/ 详情链接（`you.163.com/item/detail?id=`）。
* 已固化进 `price_collector/sources/yanxuan.py`：直连静态解析为空且 `PRICE_COLLECTOR_BROWSER=1` 时，经 `_browser.render_html` 渲染后按 `.m-product.j-product` 卡片解析（`_parse_rendered`）。端到端实测（本机 headless Chrome）稳定返回 20 条，价格准确：PokiBuds ¥179/¥299、漫步者耳夹 ¥499/¥599、纽曼 ¥89/¥129、联想 ¥59.89/¥59.9 等。
* **价格解析踩坑记录**：渲染后卡片含大量 `data-reactid=".2.0.0...$<itemId>..."` 属性，直接在卡片 HTML 上抓首个数字会误取属性内数字（全部错成 ¥2）。修复方式：先用 `re.sub(r'<[^>]+>',' ',html)` 去标签得纯文本，再按 `¥(\d+(?:\.\d+)?)` 顺序取（第 1 个现价、第 2 个原价），稳健可靠。
* `_browser.py` 增强：launch 时**优先本机真实 Chrome（`channel="chrome"`），失败回退 Playwright 自带 Chromium**，两种浏览器环境均可用。
* 环境进展：本机 Python 3.14.7 已可安装 `playwright==1.63.0`（此前无 wheel 的限制解除）；本机另装真实 Chrome 154 供渲染。
* **得物 / 抖音仍未走通（未登录）**：得物 `__NEXT_DATA__` 的 `pageProps` 为空、商品完全走客户端风控 API；抖音搜索 `loginWall=true`、仅渲染导航栏与页脚。这两个强风控平台需在浏览器内手动扫码建立登录态后再抓，未登录不重复尝试。
* 部署边界不变：EdgeOne Pages 云函数无浏览器、装不了 Playwright，**渲染抓取在本地 / Codespaces / 自有服务器运行**；EdgeOne 仍负责静态托管 + 直连/官方 API 类源。


## AI 智能引入（2026-09-25）

### 新增能力：AI 比价洞察

在「真实采集 + 规则统计」之上引入大模型，把原始商品数据转成对消费者友好的
自然语言结论。

* **`price_collector/ai.py`**：AI 核心模块（纯标准库 urllib，无 openai 依赖）。
  - `ai_insights(keyword, products, platform_stats, summary)`：把**真实采集**到的
    商品（名称/现价/原价/平台）+ 平台统计喂给模型，要求「只基于数据、不编造
    价格」，返回结构化 JSON：`headline`（一句话结论）、`best_pick`（最划算推荐
    + 理由）、`price_view`（价格区间解读）、`platform_view`（平台差异）、
    `tips`（避坑/购买建议）。
  - 兼容 **OpenAI Chat Completions 协议**，默认接火山引擎方舟（Ark）
    `https://ark.cn-beijing.volces.com/api/v3`，可经环境变量指向任意兼容端点。
  - 密钥配置（环境变量优先，回退构建时写入的 `_ai_config.py`）：
    `PC_AI_API_KEY` / `PC_AI_BASE_URL` / `PC_AI_MODEL`（另认 `ARK_*`/`OPENAI_*`）。
  - **无 key / 无数据 / 请求失败 / 返回非法 → 一律 `{"enabled":false,"reason":...}`**，
    绝不崩溃、绝不伪造；`_extract_json` 兼容裸 JSON、```json 代码块、前后带文字。
* **云函数 `cloud-functions/api/insights.py`**：`GET /api/insights?keyword=&platforms=&size=`，
  跑 pipeline → 统计 → AI 洞察。独立于 `/api/search`，前端仅在用户点击时调用，
  避免每次搜索都产生模型调用。
* **前端 AI 卡片**（index.html / app.js / style.css）：KPI 下方新增「AI 智能洞察」
  卡片 + 「AI 智能分析」按钮；未采集时按钮禁用，点击后展示结论（推荐/解读/
  避坑），无 key 时显示配置指引。AI 强调色用克制的靛蓝→teal 渐变，双主题适配。
* 配置模板 `price_collector/_ai_config.py.example`；`_ai_config.py` 已加入
  `.gitignore`，密钥绝不提交。

### 启用真实 AI（免费额度）

1. 打开火山方舟控制台 `https://console.volcengine.com/ark`，注册后创建「推理
   接入点」，选择 doubao 系列模型（新用户有免费额度）；
2. 取得 API Key 与模型/接入点 ID；
3. 写入本地 `price_collector/_ai_config.py`（复制 example），**或**直接在 EdgeOne
   项目设置环境变量（推荐，免重建）：
   `edgeone makers env set PC_AI_API_KEY "<key>" -e production`
   （如需自定义：`PC_AI_BASE_URL`、`PC_AI_MODEL`）；
4. 重新点击「AI 智能分析」即出结论。

### 线上验证结果（2026-09-25）

* 部署 deploymentId `dp30zl6l996g`（ProjectId `makers-iln0on4uoaac`，域名
  `priceprobe-full-9gjgknxf.edgeone.cool`）。
* 页面实测：33 平台 chips、AI 卡片/按钮渲染正常；只选 Amazon 搜
  "wireless earbuds" 采集 **16 条真实商品**（最低 ¥93.72 / 均价 ¥202.73 /
  最高 ¥671.30），AI 按钮随之启用。
* `/api/insights` 端点 **200**，无 key 时正确返回 `enabled:false / no_api_key`；
  前端点击显示「后端未配置 AI 密钥…」提示 —— 优雅降级链路完整。
* 配 key 后无需改代码即可出 AI 结论（环境变量热生效）。

### 本轮连接器 / 技能盘点

* **playwright-browser-automation**：驱动真实 Chrome 渲染 JS 重站（网易严选
  56 条已跑通），为 AI 提供更丰富的真实数据来源；
* **edgeone-pages**：承载前端 + 云函数（search / insights / health / netcheck）；
* 其余连接器（github / gitee / gitlab / etsy-openapi / humbird-merchant /
  todoist）与本次 AI 引入无直接数据关系，按需在对应场景使用。
* 边界：EdgeOne 云函数无浏览器、不预装 playwright，**浏览器渲染在本地 /
  Codespaces / 自有服务器运行**；AI 洞察只要云函数能访问模型 endpoint + 配好
  key 即可在线上直接生效。


## 免费 AI 落地：智谱 GLM-4-Flash（2026-09-25，永久免费）

用户要求 AI 能力完全免费。火山方舟/EdgeOne AI Gateway 均为「限时/限量」免费
额度，最终选用**智谱 GLM-4-Flash「永久免费」模型**：

| 项 | 值 |
| --- | --- |
| 注册 | `https://open.bigmodel.cn`，手机号即开（新用户另送约 2000 万 Token） |
| OpenAI 兼容 base_url | `https://open.bigmodel.cn/api/paas/v4` |
| 模型 ID | `glm-4-flash`（永久免费；想试更新多模态可用 `glm-5.3-flash`） |
| 鉴权 | `Authorization: Bearer <API Key>` |
| 官方文档 | `https://docs.bigmodel.cn/cn/guide/develop/openai/introduction` |

`ai.py` 的默认 `_DEFAULT_BASE_URL` / `_DEFAULT_MODEL` 已改为智谱免费档，**只需
配置 PC_AI_API_KEY 即可**；想换其它 OpenAI 兼容服务再用 PC_AI_BASE_URL /
PC_AI_MODEL 覆盖。`_ai_config.py.example` 同步更新为智谱免费拿 key 步骤。

**线上端到端验证（站点 priceprobe-ai，projectId makers-0mf5ypb6cisf）**：
只选 Amazon 搜 "wireless earbuds" 采集 17 条真实商品（¥93.87 / 均价 179.18 /
最高 ¥672.34），点击「AI 智能分析」由 glm-4-flash 真实生成：一句话结论、
最划算推荐（TOZO A1，价格最低性价比高）、价格区间解读、平台差异、3 条避坑
建议 —— `enabled:true, model:glm-4-flash`。

### EdgeOne 免费托管的域名边界（重要）

* EdgeOne Makers 免费版分配的 `*.edgeone.cool` 默认域名**仅提供约 3 小时预览**
  （由 eo_token 控制，过期需重新部署获取新链接）——这是平台内容合规要求。
* **要永久公开访问，必须绑定「自定义域名」**（域名约几十元/年；加速区域含
  中国大陆时域名需备案）。在控制台「域名管理 → 添加自定义域名」按提示配置
  CNAME 即可，平台自动签发并续期免费 SSL。
* 若要「托管 + 域名都永久免费」，可迁移到：
  - 国内：腾讯云 CloudBase 云开发免费环境（静态托管 + 云函数，国内访问快）；
  - 海外：Cloudflare Pages / Vercel（永久免费 `*.pages.dev` / `*.vercel.app`
    子域名、自带 Serverless，但中国大陆访问需自行解决网络）。

### 配置顺序（避坑）

环境变量在**部署时注入**函数，正确顺序：
1. `edgeone makers link -n <项目名>`（创建并绑定本地到项目）；
2. `edgeone makers env set PC_AI_API_KEY "<key>" -e production`（部署前配好）；
3. `edgeone makers deploy deploy --json`（部署并注入环境变量）；
4. 用返回的带 eo_token URL 打开即激活；token 过期重新部署即可。
